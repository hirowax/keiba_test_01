# プロジェクト現状整理（2026-08-08）

## 本日の作業サマリー

### Issue #1: JRA公式払戻スクレイパー `scrape_jra_payouts.py` — ✅完了・レビュー合格

- 担当Sonnetで実装 → Fableレビュー合格（受入基準4/4）→ 指摘3件修正 → `main`へマージ済み・push済み
- 受入基準結果:
  | 基準 | 結果 |
  |---|---|
  | プローブ（202510まで遡れるか） | ✅ 2023年7月台まで到達確認 |
  | 1日照合（netkeiba既知値との一致） | ✅ 全4件PASS |
  | `--all`バッチ取得 | ✅ 動作確認（20251004/20251005で欠落ゼロ） |
  | スリープ・cnameキャッシュ | ✅ 実装済み |
- レビュー指摘・修正:
  1. 連続失敗中断時に部分ファイルを書くと`--all`の再取得対象から外れ欠落が永久化 → 中断時は書き込まない方式に修正
  2. cnameキャッシュに失効時の無効化がない（当週系リンクは週替わりで失効し得る）→ パラメータエラー検知時にエントリ破棄+1回再クロールを追加
  3. 未使用のVENUE_MAP定数を削除
- 実装中に判明した仕様の追加事項（`docs/roi_improvement_plan.md`に記録済み）:
  - エントリーポイントはGETでは「パラメータエラー」、POSTで`cname`フィールドが必要
  - 払戻HTMLの実クラス名：単勝=`win`、複勝=`place`、枠連=`wakuren`（仕様になかったが存在）
  - 7頭立て等で枠連欄が空になる仕様のため、パース時にスキップ処理が必要
- 払戻データ取得済み: 20251004, 20251005, 20260125, 20260328, 20260404（検証用+テスト分）
- **残り: 83日分のバックフィル未実施**（`python3 scrape_jra_payouts.py --all` を複数回に分けて実行）

### インシデント: cronのpush失敗（解決済み）

- 17:00の`run.sh`（8/9分ピックアップ）実行時、リポジトリが`feat/issue-1-jra-payouts`ブランチのままだったため
  cronの`git push`が失敗（upstream未設定）。スクレイプ自体は成功しローカルコミットのみ残った
- 対応: 該当コミットを`main`へcherry-pick → push完了。8/9の注目馬データはGitHub Pagesに公開済み
- 18:30の結果cron（8/8分）は`main`に戻っていたため正常動作

## 残りタスク（優先順）

1. **払戻バックフィル**: `python3 scrape_jra_payouts.py --all` を繰り返し実行（残り83日・1回3日分ずつ）— Issue #2の前提
2. **Issue #2: 3連複の事前登録検証**（担当Opus）— バックフィル完了後「Issue #2 やって」
3. **Issue #3: ペーパートレード台帳** `paper_trade.py`（担当Sonnet）— Issue #2と並行可「Issue #3 やって」
4. **各Issue完了後**: 「Issue #N レビューして」でFableへ
5. **毎月初**: 「月次レポートやって」（Issue #3合格後から）
6. **Phase B**（EVバックテスト・Fable）— クリーンデータ90日到達後（2027-01末頃見込み）
7. **Phase C**（損益分岐オッズUI・Sonnet）— Phase B採用基準クリア後
8. **Issue #7: 実銭昇格判断** — ペーパートレード90開催日後（人間が判断）

スケジュールの一次ソースは `docs/betting_workflow_issues.md`（ステータスボード更新済み）。

## 注意事項

- ワーキングツリーに別セッション由来の未コミット変更が残っている
  （`CLAUDE.md`・`race_pickup.py`・`run_pickup_all.py`・`scrape_results.py`・`docs/anaba_*`系の未追跡ファイル多数）。
  Issue #1とは無関係のため今回は触れていない
- **作業後は必ず `main` ブランチに戻す**（cronが動く時間帯にfeatureブランチのままにしない — 今回のインシデントの再発防止）
